{
  "name": "vault_integrity_check",
  "version": "1.0.0",
  "main": "vault_data_integrity_check.js",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "tsc --project tsconfig.json",
    "postbuild": "cp package.json dist/package.json && cp yarn.lock dist/yarn.lock && cp -r node_modules dist/node_modules"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.126",
    "@types/node": "^20.9.0",
    "typescript": "^5.2.2"
  }
}
