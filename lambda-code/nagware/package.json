{
  "name": "nagware",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "biome check --error-on-warnings --diagnostic-level=warn ."
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "catalog:",
    "@aws-sdk/lib-dynamodb": "catalog:",
    "@gcforms/connectors": "catalog:",
    "@gcforms/database": "catalog:",
    "@gcforms/types": "catalog:",
    "redis": "^6.2.1"
  }
}
